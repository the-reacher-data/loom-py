"""Fake application package referenced by the ``corpus_v1`` artifacts.

Made importable by the ``fake_myapp_path`` fixture so ``module:symbol``
references in the corpus resolve without a real application.
"""
