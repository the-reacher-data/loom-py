includes:
  - conf.yaml

app:
  discovery:
    mode: interfaces
    interfaces:
      modules:
        - tests.integration.fake_repo.product.interface
      warn_recommended: true

metrics:
  enabled: true
