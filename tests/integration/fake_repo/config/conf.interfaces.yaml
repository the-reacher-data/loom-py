includes:
  - conf.yaml

app:
  discovery:
    mode: interfaces
    interfaces:
      modules:
        - tests.integration.fake_repo.product.repository
        - tests.integration.fake_repo.product.interface
      warn_recommended: true

observability:
  prometheus:
    enabled: true
    config:
      path: /metrics
